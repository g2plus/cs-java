server:
  port: 8080
spring:
  application:
    name: nacos-config-client
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        # Nacos服务注册中心地址
        server-addr: http://114.132.210.77:8848
      config:
        # 指定Nacos配置中心的地址
        server-addr: http://114.132.210.77:8848
        file-extension: yaml # 指定yaml格式的配置 默认properties
        #命名空间名称
        namespace: d03feac1-001d-422e-934e-e5f7471f6a7f
        #组名
        group: DEFAULT_GROUP
#data id的命名规则
#dataId:{spring.application.name}-{spring.profiles.active}.${file-extension}
